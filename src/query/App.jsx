import React from 'react'
import { connect } from 'react-redux'
import './App.css'

function App(props) {
  return (
    <div>fdsfvcmdsfgm</div>
  )
}

export default connect(
  (state) => {},
  (dispatch) => {}
)(App)